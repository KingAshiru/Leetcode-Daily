class Solution:
    def getNoZeroIntegers(self, n: int) -> List[int]:
        A = 1
        B = n - 1
        
        while '0' in str(B) or '0' in str(A):
            A += 1
            B -= 1
            
        return [A,B]
