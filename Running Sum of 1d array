class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        sum_ = 0
        for i, num in enumerate(nums):
            sum_ += num
            nums[i] = sum_
        
        return nums
