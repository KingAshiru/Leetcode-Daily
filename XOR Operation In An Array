class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        nums = []
        output = 0
        for i in range(n):
            nums.append((start + 2*i))
        
        for num in nums:
            output ^= num
        
        return output
